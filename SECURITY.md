# 安全说明

本项目默认在本机运行，JupyterLab 启动脚本仅监听 `127.0.0.1`，不提供局域网远程访问。

请不要提交以下内容：

- 个人答题记录和备份文件；
- Jupyter 运行时文件、Cookie、Token 或密码；
- API 密钥、访问令牌和其他凭据；
- 包含个人信息的日志或截图。

如果发现仓库中的安全问题，请不要在公开 Issue 中粘贴密钥或完整利用细节。可以先通过 GitHub 个人主页联系维护者，或提交不含敏感信息的安全说明。
